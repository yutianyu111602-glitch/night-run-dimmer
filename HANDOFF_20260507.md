# Night Run Dimmer 接手文档

更新时间：2026-05-07  
接手对象：下一位继续维护、测试、发布或扩展本项目的 AI

## 1. 这个项目是干什么的

`Night Run Dimmer` 是一个 Windows 桌面程序，用来在夜间长跑本地 agent、DeepSeek-TUI、Codex 或其他长期任务时降低屏幕亮度，并防止电脑睡眠。

核心目标：

- 同时降低所有连接屏幕的显示亮度感知。
- 保持机器不睡眠，方便 overnight agent run。
- 使用原生桌面 UI，不做网页味 dashboard。
- 有托盘图标、桌面快捷方式、发布版 exe 和 GitHub 源码仓库。

当前默认方案是软件遮罩调暗：为每个 Windows 屏幕创建一个置顶、点击穿透的黑色 overlay 窗口。这个方法稳定，适用于所有显示器。  
可选方案是 DDC/CI 硬件亮度控制，但默认关闭，因为部分显示器响应慢或不支持，容易导致卡顿。

## 2. 关键路径和入口

本地仓库：

```text
C:\code\night-run-dimmer
```

GitHub 仓库：

```text
https://github.com/yutianyu111602-glitch/night-run-dimmer
```

GitHub Release：

```text
https://github.com/yutianyu111602-glitch/night-run-dimmer/releases/tag/v0.2.0
```

本地发布 exe：

```text
C:\code\night-run-dimmer\bin\Release\net8.0-windows\win-x64\publish\NightRunDimmer.exe
```

桌面快捷方式：

```text
C:\Users\pc\Desktop\Night Run Dimmer.lnk
```

本地 release zip：

```text
C:\code\night-run-dimmer\release\NightRunDimmer-v0.2.0-win-x64.zip
```

设置文件：

```text
%AppData%\NightRunDimmer\settings.json
```

图标生成脚本：

```text
C:\code\night-run-dimmer\tools\Generate-AppIcon.ps1
```

图标源文件：

```text
C:\code\night-run-dimmer\assets\AppIcon.ico
C:\code\night-run-dimmer\assets\AppIcon.png
```

## 3. 当前 Git 和发布状态

已确认：

- 当前分支：`main`
- 远程：`origin https://github.com/yutianyu111602-glitch/night-run-dimmer.git`
- 最近发布 tag：`v0.2.0`
- GitHub release：`v0.2.0`
- Release asset：`NightRunDimmer-v0.2.0-win-x64.zip`
- 上一次检查时本地仓库状态：`## main...origin/main`，干净
- 没有打开 PR，直接在 `main` 上维护

最近提交：

```text
20d77de Ignore local release artifacts
7a50b77 Initial release of Night Run Dimmer
```

注意：接手时必须重新运行下面命令确认，因为用户或其他 agent 可能已经改动：

```powershell
git status --short --branch
git log --oneline --decorate -5
git remote -v
```

不要提交这些本地产物：

- `bin/`
- `obj/`
- `release/`
- `.vs/`
- `*.user`
- `deepseek-*.network-response`
- `deepseek-*.txt`

`.gitignore` 已经覆盖这些路径。

## 4. 当前功能状态

已实现：

- WinForms 原生桌面窗口。
- 所有屏幕的软件遮罩调暗。
- 置顶、点击穿透 overlay，不挡鼠标点击。
- 托盘图标和托盘菜单。
- 桌面快捷方式。
- 自定义 app/window/tray/shortcut 图标。
- `Night 75%` 一键夜间模式。
- `Restore` 恢复模式。
- `All-screen visual dim` 开关。
- `Hardware DDC/CI` 可选硬件亮度控制。
- `Keep system awake` 防睡眠。
- `Ctrl+Shift+D` 全局快捷键切换 visual dim。
- 关闭窗口时隐藏到托盘，托盘 `Exit` 才真正退出。
- `%AppData%\NightRunDimmer\settings.json` 持久化设置。
- `.NET 8` 发布版 exe。
- GitHub 源码、tag、release、zip asset 已发布。

主要源码文件：

```text
Program.cs
MainForm.cs
DimOverlayForm.cs
OverlayManager.cs
MonitorBrightnessService.cs
PowerManager.cs
AppSettings.cs
NightRunDimmer.csproj
```

## 5. 性能问题和已做修复

用户反馈过：程序特别卡，不能正常使用。

根因判断：

- 早期版本在 UI 线程里每 3 秒探测 DDC/CI 显示器。
- 滑块变化时同步写硬件亮度。
- 某些显示器 DDC/CI 很慢，导致窗口卡顿。

已修复：

- `RefreshMonitorStatus()` 不再调用 `hardware.Probe()`。
- DDC/CI 默认关闭，只在用户启用后工作。
- 硬件亮度写入改为 `Task.Run` 后台执行。
- 硬件亮度写入加 800ms debounce。
- 设置保存加 500ms debounce。
- UI 刷新 timer 从 3 秒改为 15 秒。

验证过：

```powershell
dotnet build
dotnet publish -c Release -r win-x64 --self-contained false -p:PublishSingleFile=true -p:PublishReadyToRun=false
```

结果：

- build 通过。
- publish 通过。
- 发布版 exe 启动后进程保持 `Responding=True`。
- 运行约 5 到 15 秒后 CPU 低，WorkingSet 约 50MB 级别。

接手后如果继续优化性能，优先不要碰 visual overlay 路径；它是当前稳定路径。DDC/CI 仍然应保持 opt-in。

## 6. 已验证和未验证

已确认：

- 本地源码存在于 `C:\code\night-run-dimmer`。
- GitHub 仓库和 release 已创建。
- release zip 已上传。
- 发布 exe 已生成。
- 桌面快捷方式已创建，目标指向发布 exe。
- app icon 已嵌入 csproj。
- 托盘 icon 使用 exe associated icon。
- overlay 方案在设计上支持所有 Windows screens。
- 之前检测到 3 个屏幕：`DISPLAY6`、`DISPLAY7`、`DISPLAY8`。

未完全验证：

- 没有在最终接手文档阶段再次手动点击 `Night 75%`，避免突然调暗用户屏幕。
- 没有最终验证每台物理显示器的 DDC/CI 写入效果。
- 没有做安装包或代码签名。
- 没有配置 GitHub Actions 自动构建 release。
- 没有验证 Windows 任务栏图标缓存刷新问题，因为这个项目的快捷方式图标已经设置为 exe icon，但 Windows 可能缓存旧图标。

接手时建议先做无破坏确认：

```powershell
Get-Process NightRunDimmer -ErrorAction SilentlyContinue
Test-Path "C:\Users\pc\Desktop\Night Run Dimmer.lnk"
Test-Path "C:\code\night-run-dimmer\bin\Release\net8.0-windows\win-x64\publish\NightRunDimmer.exe"
```

如需人工验屏幕调暗，先告诉用户会短暂调暗所有屏幕，再启动 exe 测试 `Night 75%` 和 `Restore`。

## 7. 构建、运行、发布命令

生成图标：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tools\Generate-AppIcon.ps1
```

开发构建：

```powershell
dotnet build
```

发布 exe：

```powershell
dotnet publish -c Release -r win-x64 --self-contained false -p:PublishSingleFile=true -p:PublishReadyToRun=false
```

运行发布版：

```powershell
C:\code\night-run-dimmer\bin\Release\net8.0-windows\win-x64\publish\NightRunDimmer.exe
```

检查进程响应：

```powershell
Get-Process NightRunDimmer | Select-Object ProcessName,Id,Responding,CPU,WorkingSet64
```

检查快捷方式目标：

```powershell
$shell = New-Object -ComObject WScript.Shell
$lnk = $shell.CreateShortcut("C:\Users\pc\Desktop\Night Run Dimmer.lnk")
$lnk.TargetPath
$lnk.IconLocation
```

## 8. 用户偏好和约束

用户明确偏好：

- 要原生桌面程序，不要网页味 UI。
- 能同时降低所有屏幕亮度。
- 要托盘，并且托盘也要有图标。
- 要桌面快捷方式和图标。
- 要编译成 exe。
- 要发布源码到 GitHub。
- README、版本号可以由 AI 自己决定。
- 这个工具是为了夜间长跑 agent 时降低屏幕亮度。

接手时请遵守：

- 不要读 secrets、tokens、cookies、`.env`、SSH key，除非用户明确要求。
- 不要做 destructive git 操作。
- 不要把 `bin/`、`obj/`、`release/` 提交进仓库。
- 不要默认启用 DDC/CI 硬件亮度控制。
- UI 保持桌面工具风格，不要改成 web dashboard。

## 9. 推荐下一步

最高价值下一步：

1. 启动发布 exe，手动验证 `Night 75%`、`Restore`、托盘隐藏、托盘退出、`Ctrl+Shift+D`。
2. 如果任务栏或快捷方式图标仍显示旧图标，刷新 Windows icon cache 或重建快捷方式。
3. 做一个简单安装脚本或 MSI/NSIS installer。
4. 加 `Start with Windows` 选项。
5. 加 per-monitor dim percent，仍以 overlay 为主。
6. 加 GitHub Actions：push tag 时自动 build zip 和创建 release。
7. 如果用户需要更真实硬件亮度，再单独做 DDC/CI 设备兼容性页和超时控制。

不建议马上做：

- 不建议把 DDC/CI 设为默认开。
- 不建议引入 Electron、WebView、Next.js 或网页 UI。
- 不建议在没有用户确认时自动调暗屏幕测试。
- 不建议加入复杂 theme 或 dashboard。

## 10. DeepSeek-TUI 相关背景

这个项目最初是为了测试 `DeepSeek-TUI` 写代码能力而创建。相关背景：

- 用户想用 DeepSeek-TUI 做 coding agent。
- 用户想用 DeepSeek v4 pro 做规划指挥，多个 DeepSeek flash 做低成本执行。
- 另有一个本地前端项目 `C:\code\deepseek-tui-web`，URL 曾为 `http://127.0.0.1:43117`，runtime 曾为 `http://127.0.0.1:7878`。
- DeepSeek-TUI Web 的输入框中文 IME 问题已通过 native uncontrolled `<textarea>` 修过。
- DeepSeek-TUI Web 后来加过 `New thread` 和上下文裁剪，避免上下文膨胀。

这些背景不属于 `Night Run Dimmer` 主项目，但如果下一位 AI 要继续用户的 6-agent / DeepSeek-TUI 工作流，可以从这些线索继续查。

## 11. 接手建议

如果下一位 AI 的任务是继续维护这个 app，建议从这里开始：

```powershell
Set-Location C:\code\night-run-dimmer
git status --short --branch
dotnet build
dotnet publish -c Release -r win-x64 --self-contained false -p:PublishSingleFile=true -p:PublishReadyToRun=false
```

然后再根据用户当前请求决定是否启动 exe 做手动验证。

如果用户只是问“这是什么”，直接回答：

> 这是一个 Windows 夜间长跑调暗工具。它用原生桌面 UI 和托盘控制，在长时间跑 AI agent 时同时给所有显示器加软件暗屏遮罩，并可防止电脑睡眠；DDC/CI 硬件亮度是可选项，默认不用，避免卡顿。

